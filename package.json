{
  "main": "server.js",
  "scripts": {
    "start": "nodemon server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "fred-api": "^2.0.1",
    "nodemon": "^3.0.1"
  },
  "nodemonConfig": {
    "env": {
      "PORT": 5000
    }
  }
}
