{
  "main": "server.js",
  "scripts": {
    "start": "nodemon server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "axios": "^1.4.0",
    "express": "^4.18.2",
    "nodemon": "^3.0.1"
  },
  "nodemonConfig": {
    "env": {
      "PORT": 5000
    }
  }
}
